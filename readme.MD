```bash sudo dpkg --configure -a && sudo apt --fix-broken install -y && sudo apt update && sudo apt install -y python3 python3-pip python3-venv git curl ca-certificates gnupg && curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - && sudo apt install -y nodejs && sudo npm install -g pm2 && git clone https://github.com/AstroVoidHostDev/spywebsite.git && cd spywebsite && python3 -m venv myenv && source myenv/bin/activate && pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && pm2 start app.py --name spywebsite --interpreter ./myenv/bin/python3 && pm2 save && sudo pm2 startup systemd -u $USER --hp $HOME```
